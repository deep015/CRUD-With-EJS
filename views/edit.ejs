<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body class=" bg-zinc-900 h-full">
    <div class="w-full p-10 text-white">
        <div class="nav mb-10 text-zinc-500">
            <a href="/read">Read users</a>
        </div>
        <h1 class="text-3xl tracking=-tighter mb-3">Update User</h1>
        <form action="/update/<%= user._id%>" method="post">
            <input name="name" type="text" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent " placeholder="name" value="<%=user.name %>">
            <input name="email" type="text" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent " placeholder="email" value="<%=user.email %>">
            <input name="image" type="url" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent " placeholder="image-url" value="<%=user.image %>">
            <input name="" type="submit" value="create-user" class="px-5 py-2 text-black bg-yellow-500 rounded-lg ml-3">
        </form>
    </div>
</body>

</html>